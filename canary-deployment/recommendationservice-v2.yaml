apiVersion: apps/v1
kind: Deployment
metadata:
  name: recommendationservice-v2
  labels:
    app: recommendationservice
    version: v2
spec:
  replicas: 1  # Moins de replicas pour commencer
  selector:
    matchLabels:
      app: recommendationservice
      version: v2
  template:
    metadata:
      labels:
        app: recommendationservice
        version: v2
    spec:
      serviceAccountName: recommendationservice
      terminationGracePeriodSeconds: 5
      securityContext:
        fsGroup: 1000
        runAsGroup: 1000
        runAsNonRoot: true
        runAsUser: 1000
      containers:
      - name: server
        securityContext:
          allowPrivilegeEscalation: false
          capabilities:
            drop:
              - ALL
          privileged: false
          readOnlyRootFilesystem: true
        image: us-central1-docker.pkg.dev/google-samples/microservices-demo/recommendationservice:v0.10.3
        ports:
        - containerPort: 8080
        readinessProbe:
          periodSeconds: 5
          grpc:
            port: 8080
        livenessProbe:
          periodSeconds: 5
          grpc:
            port: 8080
        env:
        - name: PORT
          value: "8080"
        - name: PRODUCT_CATALOG_SERVICE_ADDR
          value: "productcatalogservice:3550"
        - name: DISABLE_PROFILER
          value: "1"
        - name: VERSION
          value: "v2"  # Variable pour identifier la version
        resources:
          requests:
            cpu: 50m
            memory: 220Mi
          limits:
            cpu: 200m
            memory: 450Mi
